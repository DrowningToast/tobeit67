################## Auto generated ##################
[databases]
tobeit = host=localhost port=5432 user=tobeit

[pgbouncer]
listen_addr = 0.0.0.0
listen_port = 5432
unix_socket_dir =
user = postgres
auth_file = /etc/pgbouncer/userlist.txt
auth_type = md5
pool_mode = transaction
ignore_startup_parameters = extra_float_digits

# Log settings
admin_users = postgres,tobeit

# Connection sanity checks, timeouts

# TLS settings

# Dangerous timeouts
################## end file ##################
